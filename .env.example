# Lambda Function Configuration
# Copia este archivo a .env y configura tu Lambda function

# Nombre de la Lambda function en AWS
# Por defecto: apigateway-resource-creator-ci
LAMBDA_FUNCTION_NAME=apigateway-resource-creator-ci

# Región de AWS (opcional, por defecto us-east-1)
# AWS_REGION=us-east-1

# AWS Profile (opcional)
# AWS_PROFILE=default
